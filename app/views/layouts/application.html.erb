<!DOCTYPE html>
<html>
  <head>
    <title>WanderpathJourney</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# --- 読み込み順を修正 --- %>
    <%# 1. LeafletのCSSを先に読み込む %>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
     integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
     crossorigin=""/>
    
    <%# 2. アプリケーションのCSS %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# 3. JavaScript(Turbo, Stimulus) %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <%# --- ここからナビゲーションバーを追加 --- %>
    <nav class="bg-white shadow-md">
      <div class="container mx-auto px-4">
        <div class="flex justify-between items-center py-4">
          <%= link_to 'Wanderpath Journey', root_path, class: "text-xl font-bold text-gray-800" %>
          <div class="space-x-4">
            <%= link_to '旅行一覧', trips_path, class: "text-gray-600 hover:text-blue-500" %>
            <%= link_to 'スポット管理', spots_path, class: "text-gray-600 hover:text-blue-500" %>
          </div>
        </div>
      </div>
    </nav>
    <%# --- ここまでナビゲーションバー --- %>

    <main class="container mx-auto mt-8 px-4">
      <%= yield %>
    </main>
  </body>
</html>
